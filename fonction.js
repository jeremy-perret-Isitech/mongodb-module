// geoJsonArray = [];

// data.forEach(app => {
//         if(app.localisation.geolocalisation.geoJson){{
//             geoJsonArray.push(JSON.parse(`{"type": "Feature","properties": {},"geometry": ${JSON.stringify(app?.localisation?.geolocalisation?.geoJson)}}`))
//         }}
// });

// console.log((geoJsonArray));

// data.forEach(app => {
//     notesClients = [];
//     if (app.metadonnees !== undefined) {
//         {
//             for (i = 0; i < app.metadonnees[0].contenus[0].metadonnee.nombreAvis; i++) {
//                 notesClients.push(Math.floor(Math.random() * 10));
//             }
//         }
//         app.metadonnees[0].contenus[0].metadonnee.avisClients = notesClients;
//     }
// });
// console.log(data);